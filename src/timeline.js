'use strict'

var d3 = require('d3')

var timeline = function () {
  function timeline () {
  }

  timeline.size = function (range) {
  }

  timeline.nodes = function (data) {
  }

  timeline.links = function (nodes) {
  }

  return timeline
}

module.exports = timeline
